#include "main.h"

 /**
 * add - adds two numbers
 * @a: the first number
 * @b: the second number
 * Return: the addition of both
 * Description: give two integers, returns sum
 */
int add(int a, int b)
{
	return (a + b);
}
